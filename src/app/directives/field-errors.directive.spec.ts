import { FieldErrorsDirective } from './field-errors.directive';

describe('FieldErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
