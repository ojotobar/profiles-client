import { TitlecasedPipe } from './titlecased.pipe';

describe('TitlecasedPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecasedPipe();
    expect(pipe).toBeTruthy();
  });
});
