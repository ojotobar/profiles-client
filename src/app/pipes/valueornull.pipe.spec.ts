import { ValueornullPipe } from './valueornull.pipe';

describe('ValueornullPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueornullPipe();
    expect(pipe).toBeTruthy();
  });
});
